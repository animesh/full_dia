name: Run all tests

on:
  push:
    branches:
      - "**"       # 所有分支
  pull_request:
    branches:
      - "**"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install full_dia
        run: |
          python -m pip install --upgrade pip
          pip install .
          pip install torch=2.3
          pip install cupy-cpu
          pip install pytest     

      - name: Run tests
        run: |
          pytest tests/           
