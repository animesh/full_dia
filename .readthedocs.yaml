version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  extra_commands:
    - sphinx-apidoc -o docs/api_reference full_dia --force --separate

python:
  install:
    - requirements: requirements/docs.txt

sphinx:
  configuration: docs/conf.py